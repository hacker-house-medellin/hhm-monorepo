# Contributing

This repository was bootstrapped for `hacker-house-medellin` as part of the `monorepo` repo family.

## Local checks

```bash
npm test
```

## Branching

Use `agent/<change-summary>` for generated implementation branches and open draft PRs against `main`.
